apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aster-license-signing-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  # 可选 Slack webhook；不要在 values.yaml 明文提交生产值
  LICENSES_SLACK_WEBHOOK: {{ .Values.secrets.licensesSlackWebhook | quote }}
