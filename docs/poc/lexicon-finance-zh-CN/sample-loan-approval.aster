模块 金融.信贷.审批。

金融定义 申请人 包含：
  KYC 等级 是 整数，
  征信分数 是 整数，
  负债收入比 是 浮点数，
  反洗钱标记 是 布尔值。

金融规则 评估授信 给定 申请人，产出：
  如果 申请人.反洗钱标记 是 真：
    返回 「拒绝-AML 命中」。
  否则 如果 申请人.KYC 等级 小于 2：
    返回 「拒绝-KYC 不足」。
  否则 如果 申请人.征信分数 小于 600：
    返回 「拒绝-征信不足」。
  否则 如果 申请人.负债收入比 大于 0.5：
    返回 「拒绝-DTI 过高」。
  否则：
    返回 「通过」。
